import { createContext } from 'react';
import type { Alignment } from '../utils/professionalEditing';
import type { EditorLayout, EditorPresent, EditorTool, PdfAnnotation } from '../types/annotations';

export type PdfEditorValue = { activeTool: EditorTool; selectedId: string | null; selectedIds: string[]; annotationsByPageId: Record<string, PdfAnnotation[]>; formValues: EditorPresent['formValues']; flattenForms: boolean; layout: EditorLayout; history: EditorPresent[]; dirty: boolean; canUndo: boolean; canRedo: boolean; setTool: (tool: EditorTool) => void; select: (id: string | null, append?: boolean) => void; selectMany: (ids: string[]) => void; add: (annotation: PdfAnnotation) => void; update: (id: string, patch: Partial<PdfAnnotation>) => void; remove: (id: string) => void; removeSelected: () => void; duplicate: (id: string) => void; duplicateSelected: () => void; alignSelected: (alignment: Alignment) => void; updateLayout: (patch: Partial<EditorLayout>) => void; setFormValue: (name: string, value: string | boolean | string[]) => void; setFlattenForms: (value: boolean) => void; undo: () => void; redo: () => void; copy: (id: string) => void; paste: (pageId: string) => void; reorder: (id: string, direction: 'forward' | 'backward') => void };
export const PdfEditorContext = createContext<PdfEditorValue | null>(null);
