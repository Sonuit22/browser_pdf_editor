import { ArrowRight, Check, ShieldCheck } from 'lucide-react';
import { Link } from 'react-router-dom';
import { BrandLockup } from '../components/Brand';

const groups = [{ title: 'Edit', items: ['Add text', 'Add image', 'Highlight', 'Draw', 'Shapes', 'Sign'] }, { title: 'Organize', items: ['Merge', 'Split', 'Reorder', 'Extract', 'Rotate', 'Delete', 'Duplicate'] }, { title: 'Convert', items: ['PDF to images', 'Images to PDF', 'PDF to text', 'Text to PDF'] }, { title: 'Privacy', items: ['Browser-based', 'Local processing', 'No server upload', 'No forced watermark'] }];

export default function HomePage() {
    return <div className="product-home"><section className="product-hero" aria-labelledby="product-title"><BrandLockup /><h1 id="product-title" className="sr-only">PDF Editor by ib</h1><p className="privacy-tagline" aria-live="polite">Your data privacy is our concern. No server upload required.</p><div className="privacy-badge"><ShieldCheck size={18} aria-hidden="true" /><span><strong>Browser-based processing</strong>Files stay on your device</span></div><div className="hero-actions"><Link className="button button--primary" to="/edit">Open editor <ArrowRight size={17} aria-hidden="true" /></Link><Link className="button button--secondary" to="/convert">Explore conversions</Link></div></section><section className="home-features" aria-labelledby="free-tools-title"><div className="section-heading"><div><p className="eyebrow">Free daily-use tools</p><h2 id="free-tools-title">Useful PDF work without artificial limits</h2></div><Link className="text-link" to="/pricing">Free and premium details <ArrowRight size={17} aria-hidden="true" /></Link></div><div className="feature-groups">{groups.map((group) => <section key={group.title}><h3>{group.title}</h3><ul>{group.items.map((item) => <li key={item}><Check size={15} aria-hidden="true" />{item}</li>)}</ul></section>)}</div></section></div>;
}
